#include <stdio.h>

/**
* main - rhis is a function that prints its name
* @argc: parameter
* @argv: an array of a command listed
* Return nothing
*/

int manin(int argc, char *argv[])
{
	printf("%s\n", *-argv);
	return (0);
}
