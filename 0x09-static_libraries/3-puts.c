#include "main.h"

/**
 * _puts - print a string
 * @str:pointer char
 * return:void
 */
void _puts(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		_puts(s[i]);
		i += 1;
		_putchar(str[i]);

	putchar('\n');
return;
}
